# Sample Project

This is a sample project for testing EZSQL's `find_context` tool.

## Schema

- `users` table: user accounts
- `orders` table: user orders with FK to users

## Migrations

- `001_init.sql`: initial schema
- `002_add_orders.sql`: orders table
